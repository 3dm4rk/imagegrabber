# imagegrabber
grab uploaded image


How to Run It

Extract the ZIP.

Open terminal inside the extracted folder.

Run:

npm install
node server.js

Open in browser:

http://localhost:3000

The uploaded images will automatically be saved inside:

public/img/
